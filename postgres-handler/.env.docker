# PostgreSQL 股票数据处理服务 - Docker 部署配置

# 数据库配置
DB_PASSWORD=fintrack123
DB_PORT=5432

# 应用服务配置
PORT=8080

# pgAdmin 配置（可选）
PGADMIN_EMAIL=admin@fintrack.com
PGADMIN_PASSWORD=admin123
PGADMIN_PORT=5050

# 部署说明：
# 1. 复制此文件为 .env: cp .env.docker .env
# 2. 根据需要修改密码和端口
# 3. 启动服务: docker-compose up -d
# 4. 启动包含pgAdmin: docker-compose --profile admin up -d